# Core (Phase 3) — orchestrator + realtime writes
fastapi>=0.115
uvicorn[standard]>=0.30
supabase>=2.7
python-dotenv>=1.0
pydantic>=2.8

# Real adapters — Browser Use agent + cloakbrowser stealth Chromium (over CDP)
browser-use>=0.13
cloakbrowser>=0.4          # stealth browser; run: python -m cloakbrowser install
